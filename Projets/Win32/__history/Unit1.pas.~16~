unit Unit1;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.ToolWin, Vcl.ComCtrls, Vcl.Menus,
  Vcl.StdCtrls, Vcl.ExtCtrls, Vcl.JumpList, System.Win.TaskbarCore, Vcl.Taskbar,
  Vcl.TitleBarCtrls;

type
  TForm1 = class(TForm)
    saisie_1: TEdit;
    Label1: TLabel;
    Label2: TLabel;
    saisie_2: TEdit;
    Bouton_Ajouter: TButton;
    Bouton_Multiplier: TButton;
    Bouton_Soustraire: TButton;
    Bouton_Diviser: TButton;
    somme: TLabel;
    procedure Button1Click(Sender: TObject);
    procedure saisie_1Change(Sender: TObject);
    procedure Label3Click(Sender: TObject);
    procedure Bouton_AjouterClick(Sender: TObject);
  private
    { Déclarations privées }
  public
    { Déclarations publiques }
  end;

var
  Form1: TForm1;

implementation

{$R *.dfm}

procedure TForm1.Bouton_AjouterClick(Sender: TObject);
begin

end;

procedure TForm1.Button1Click(Sender: TObject);

var numero1, numero2, resultat : integer ;
begin

numero1 := StrToInt(saisie_1.Text) ;
numero2 := StrToInt(saisie_2.Text) ;
resultat := numero1 + numero2 ;

somme.Caption := IntToStr(resultat) ;


end;

procedure TForm1.Label3Click(Sender: TObject);
begin

end;

procedure TForm1.saisie_1Change(Sender: TObject);
begin

end;

end.

